# OpenTofu / Terragrunt runtime artifacts
.terraform/
.terragrunt-cache/
*.tfstate
*.tfstate.*
*.tfplan
.tflock
crash.log

# Local env / secrets
.env
*.tfvars.local

# OS / editor noise
.DS_Store

# devcontainer-generated tasks (copied from kube-devenv image on start)
.vscode/tasks.json

# Committed lockfile stays tracked — do NOT ignore .terraform.lock.hcl

# Agent-managed worktree scratch space
.worktrees/
